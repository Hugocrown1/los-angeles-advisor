import UserForm from "@/components/UserForm";
import React from "react";

const page = () => {
  return (
    <main className="flex flex-col items-center justify-center gradient-bg h-[100vh]">
      <UserForm />
    </main>
  );
};

export default page;
